# 1. Usamos una imagen de Nginx como base
FROM nginx:alpine

# 2. Copiamos tus archivos locales a la carpeta donde Nginx busca contenido
COPY . C:\Users\Ferna\OneDrive\Escritorio\webgym

# 3. Exponemos el puerto 80 (el estándar para web)
EXPOSE 80